// Copie este arquivo para "config.js" (mesma pasta) e preencha com os valores do seu
// projeto Supabase — Project Settings > API no painel do Supabase.
//
// config.js é o arquivo que o site realmente lê (onboarding-cliente.html carrega ele
// antes do script principal). Não é enviado para nenhum lugar: fica só na sua máquina.

window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
